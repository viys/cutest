interface:
  display_name: "CuTest Unit Test Builder"
  short_description: "Add or adapt CuTest-based tests in C repositories"
  default_prompt: "Use $cutest-unit-test-builder to inspect this repository and add or update CuTest-based unit tests."

policy:
  allow_implicit_invocation: true
